from django.urls import path
# from account.views import login, logout, reg

app_name = 'backend'

servers = ['1', '2', '3', '4', '5']

urlpatterns = [
    # path('login/', login),
    # path('logout/', logout),
    # path('register/', reg),
]
